<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.PinLite" parent="Theme.Material3.Light.NoActionBar">

        <!-- Warna status bar -->
        <item name="android:statusBarColor">@color/red_pinlite</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Warna background default semua halaman -->
        <item name="android:windowBackground">@color/white</item>
        <item name="colorPrimary">@color/red_pinlite</item>
        <item name="colorOnPrimary">@color/white</item>

    </style>
</resources>
